MUTAG dataset:
self.conv1 = GATConv(dataset.num_features, 1, heads=32, dropout=0.6, concat=False)
self.conv4 = GATConv(1 * 1, 1, heads=32, dropout=0.6)

self.fc1 = Linear(32, 16)
self.fc2 = Linear(16, dataset.num_classes)

if epoch == 51 and epoch<400:
	for param_group in optimizer.param_groups:
	    param_group['lr'] = 0.5 * param_group['lr']

epoch:600
lr:0.01
